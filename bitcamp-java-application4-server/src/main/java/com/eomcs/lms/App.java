package com.eomcs.lms;
// 56_4: 포워딩과 인클루딩 활용
public class App {
  
  // 작업:
  // =>
  // => ErrorServlet 생성
  // => 등록, 변경, 삭제 중 오류 발생할때 오류 내용을 출력하는 ErrorServlet으로 서블릿으로 포워딩 하기
  // => HeaderServlet 생성
  // => FooterServlet 생성
  // => 모든 출력에 헤더와 풋터 서블릿의 출력를 인클루딩하기
  
  // dummy 클래스
  // =>  기존버전에서 계속 존재했던 클래스라서 그대로 둠!
  // => 단지 버전의 목표에 대한 설명을 기록하기 위해 존재함.
  // => 프로젝트에서 사용되지 않음.
}
